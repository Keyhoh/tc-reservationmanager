package reservationmanager.domain.reserve;

import reservationmanager.domain.guest.Guest;
import reservationmanager.domain.lodge.Lodge;

public class Reserve {
    private Guest guest;
    private Lodge lodge;
}
